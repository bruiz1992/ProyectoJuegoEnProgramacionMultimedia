using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pulsar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //BOTONES PRINCIPALES
        if (Input.GetKey(KeyCode.JoystickButton0))
        Debug.Log("has pulsado A / EQUIS");

        if (Input.GetKey(KeyCode.JoystickButton1))
        Debug.Log("has pulsado B / CIRCULO");

        if (Input.GetKey(KeyCode.JoystickButton2))
        Debug.Log("has pulsado X / CUADRADO");

        if (Input.GetKey(KeyCode.JoystickButton3))
        Debug.Log("has pulsado Y / TRIANGULO");

        if (Input.GetKey(KeyCode.JoystickButton4))
        Debug.Log("has pulsado L1");

        if (Input.GetKey(KeyCode.JoystickButton5))
        Debug.Log("has pulsado R1");

        if (Input.GetKey(KeyCode.JoystickButton6))
        Debug.Log("has pulsado SHARE / SELECT");

        if (Input.GetKey(KeyCode.JoystickButton7))
        Debug.Log("has pulsado BARRITAS / START");

        if (Input.GetKey(KeyCode.JoystickButton8))
        Debug.Log("has pulsado L3");

        if (Input.GetKey(KeyCode.JoystickButton9))
        Debug.Log("has pulsado R3");
///aaaaaaaaa


   //     if (Input.GetAxis("Horizontal"))
     //   Debug.Log("1000");




        if (Input.GetKey(KeyCode.JoystickButton10))
        Debug.Log("10");

        if (Input.GetKey(KeyCode.JoystickButton11))
        Debug.Log("11");

        if (Input.GetKey(KeyCode.JoystickButton12))
        Debug.Log("12");

        if (Input.GetKey(KeyCode.JoystickButton13))
        Debug.Log("13");

        if (Input.GetKey(KeyCode.JoystickButton14))
        Debug.Log("14");

        if (Input.GetKey(KeyCode.JoystickButton15))
        Debug.Log("15");

        if (Input.GetKey(KeyCode.JoystickButton16))
        Debug.Log("16");

         if (Input.GetKey(KeyCode.JoystickButton17))
        Debug.Log("17");

        if (Input.GetKey(KeyCode.JoystickButton18))
        Debug.Log("18");

        if (Input.GetKey(KeyCode.JoystickButton19))
        Debug.Log("19");

       /* if (Input.GetKey(KeyCode.JoystickButton2))
        Debug.Log("20");

        if (Input.GetKey(KeyCode.JoystickButton14))
        Debug.Log("21");

        if (Input.GetKey(KeyCode.JoystickButton15))
        Debug.Log("22");

        if (Input.GetKey(KeyCode.JoystickButton16))
        Debug.Log("23");

*/

    }
}
